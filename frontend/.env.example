# Vite environment variables for the Lab 3 React SPA.
# Copy this file to .env.local (git-ignored) and edit values for your machine.

# The backend the Vite dev server should proxy /api/* to.
# - Spec / teacher's machine: http://localhost:8001
# - Anastasia's Mac (Docker holds 8001): http://localhost:8002
VITE_API_TARGET=http://localhost:8001
